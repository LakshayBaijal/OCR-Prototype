# ---------------------------------------------------------------- Setup (one-time)
# Barcode decoding uses TWO decoders - cv2 (bundled) and ZBar (better; catches bars cv2
# misses or misdecodes outright). ZBar needs its native library installed system-wide:
#     brew install zbar
# Optional: barcode.py falls back to cv2-only if this isn't installed. `pip install pyzbar`
# (in requirements.txt) is the Python wrapper; brew installs the C library it wraps.

# ---------------------------------------------------------------- Stage 0 + 1 UI
streamlit run app.py


# ---------------------------------------------------------------- Local VLM (Tier 2, FREE)
# Start this ONCE, in its own terminal, and leave it running.
# The model loads once and stays resident; every script then just POSTs to localhost.
# Importing mlx_vlm in-process instead meant a ~130s cold load per script AND each process
# carrying 3-4GB of weights - which starved the machine and crashed VS Code.
#
# NOTE: use gemma-3. The ENTIRE Qwen-VL family (Qwen2.5-VL AND Qwen3-VL, both tested)
# crashes under mlx_vlm.server with "RuntimeError: There is no Stream(gpu, N) in current
# thread" - the identical get_rope_index() code in qwen2_5_vl/language.py and
# qwen3_vl/language.py touches the GPU from the server's worker thread. This is a shared
# mlx-vlm library bug, not a per-model issue - don't retry any Qwen-VL model here until
# mlx-vlm fixes it upstream. Gemma-3 is the only VLM family confirmed to serve correctly.
#
# mlx_vlm.server supports several cached models and lazy-swaps between them per request
# (unload current, load requested) - so switching models is just changing MODEL in
# ocr/local_vlm.py, no server restart needed. But /v1/models lists EVERY model ever
# cached to disk, not "what's currently loaded" - do not infer the active model from it.
#
# Gemma-4 (released 2026-04-02) is NOT usable here yet, tested on mlx-vlm 0.6.4:
# mlx-community/gemma-4-E4B-it-qat-4bit fails to LOAD outright - "Failed to load model:
# Expected shape (128, 3, 3, 1) but received shape (128, 3, 1, 3) for parameter
# audio_tower.subsample_conv_projection.layer0.conv.weight" (a real mlx-vlm bug in how it
# transposes Gemma-4's audio-tower weights, not a config issue on our end). The community
# has ALSO separately reported naive 4-bit MLX quants of Gemma-4 (non-QAT) producing
# garbage output from a PLE-layer quantization bug. Don't retry Gemma-4 here until
# mlx-vlm fixes both upstream - same category as the Qwen-VL bug above.

- For Gemma 3 Quantised to 4 bits
venv/bin/python -m mlx_vlm.server \
    --model mlx-community/gemma-3-4b-it-4bit \
    --port 8080 --kv-bits 8 --max-tokens 512




- Evaluations

Step 1 — no server needed (run these now):


cd "/Users/lakshay.baijal/Desktop/Projects/OCR Prototype"

# Context first: what's the recall CEILING (what's photographed at all)?
venv/bin/python ceiling.py --n 200

# Headline: does Stage 0 preprocessing beat raw? Field recall vs GT, RapidOCR.
venv/bin/python evaluate_ocr.py --n 200

# Is low recall the engine or the feeding? (FSSAI probe)
venv/bin/python tune_ocr.py

# Barcode pipeline end-to-end vs GT UPC/EAN

Step 2 — start the MLX VLM server once (own terminal, leave it running):


venv/bin/python -m mlx_vlm.server \
    --model mlx-community/gemma-3-4b-it-4bit \
    --port 8080 --kv-bits 8 --max-tokens 512
Step 3 — with the server up (the full engine comparison):


cd "/Users/lakshay.baijal/Desktop/Projects/OCR Prototype"

# Every free engine head-to-head vs GT: rapidocr / easyocr / local_vlm / union
venv/bin/python benchmark_models.py --n 100

# Same, on RAW images — proves Stage 0 earns its keep
venv/bin/python benchmark_models.py --n 100 --raw

# Field recall WITH the union VLM tier on
venv/bin/python evaluate_ocr.py --n 200 --vlm

# RapidOCR vs VLM on the images RapidOCR struggles with
venv/bin/python compare_engines.py --n 200





# ---------------------------------------------------------------- Stage 2 (Layout / Field Parser)
# Turns Stage-1 OCR (text + boxes + confidence) + the decoded barcode into the structured
# ProductExtraction schema (Brand / MRP / Net Qty / FSSAI / UPC / Manufacturer / Country / Name).
#
# The heuristic (regex + layout) parser works with NO training. Score it vs Ground Truth.csv:
venv/bin/python eval_stage2.py --n 100
venv/bin/python eval_stage2.py --n 100 --vlm        # union the local VLM into Stage-1 OCR first



# ---- Model training LayoutLMv3 tier ----
# microsoft/layoutlmv3-base has NO product-field head, so it must be FINE-TUNED first. We have
# no token labels, so training uses WEAK SUPERVISION (aligns GT field values onto OCR tokens).
# 1) Train (writes layoutlmv3_finetuned/). Smoke-test the whole loop first:
venv/bin/python train_layoutlm.py --n 20 --epochs 1          # fast end-to-end check
venv/bin/python train_layoutlm.py --n 300 --epochs 3         # a real run
# 2) Then evaluate WITH the LayoutLMv3 tier merged in on top of the heuristic parser:
venv/bin/python eval_stage2.py --n 100 --layoutlm


cd "/Users/lakshay.baijal/Desktop/Projects/OCR Prototype"
venv/bin/python train_layoutlm.py --n 300 --epochs 3 

venv/bin/python eval_stage2.py --n 100 --layoutlm